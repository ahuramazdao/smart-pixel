Natywny select w skórze systemu (mono etykieta, czerwona strzałka ↓).

```jsx
<Select label="Czego potrzebujesz?" options={['Strona www','Aplikacja webowa','Automatyzacja']} value={v} onChange={e => setV(e.target.value)} />
```
